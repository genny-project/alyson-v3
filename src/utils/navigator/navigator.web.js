/**
 * TODO:
 *
 * Currently a stump on web
 */

const navigator = {
  setParams: () => {},
  setTopLevelAppNavigator: () => {},
  setTopLevelAuthNavigator: () => {},
  navigate: () => {},
};

export default navigator;
