const formatAddress = ( address, format, options = {}) => { // eslint-disable-line no-unused-vars
  // const {} = options;

  if ( address == null ) return 'Invalid address';
  if ( format == null ) return address;

  return address;
} ;

export default formatAddress;
