import React, { Component } from 'react';
import { Text } from '../../components';

class LayoutEditor extends Component {
  render() {
    return (
      <Text>
        Layout editor isn't currently supported on Native devices.
      </Text>
    );
  }
}

export default LayoutEditor;
