import React, { PureComponent } from 'react';
import { Box, Text } from '../../components';

class WebView extends PureComponent {
  componentDidMount() {
    console.error( 'TODO Implement WebView web' );
  }

  render() {
    return (
      <Box opacity={0.4}>
        <Text>
          TODO Webview
        </Text>
      </Box>
    );
  }
}

export default WebView;
